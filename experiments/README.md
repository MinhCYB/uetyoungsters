# Experiments

Experimental collector prototypes. **Not used in production** and excluded
from Docker builds.

- `topcv/` — TopCV collector prototype (requires Playwright; access challenges unresolved).
